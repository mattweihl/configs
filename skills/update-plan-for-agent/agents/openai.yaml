interface:
  display_name: "Update Plan for Agent"
  short_description: "Expand a plan for another coding agent"
  default_prompt: "Use $update-plan-for-agent to turn this plan into verified implementation steps."
policy:
  allow_implicit_invocation: false
